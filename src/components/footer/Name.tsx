import { chakra, HTMLChakraProps, IconButton, useToken } from "@chakra-ui/react";
import * as React from "react";

export const Name = (props: HTMLChakraProps<"svg">) => {
  const [white, black] = useToken("colors", ["white", "gray.800"]);
  return (
    
  
  );
};
